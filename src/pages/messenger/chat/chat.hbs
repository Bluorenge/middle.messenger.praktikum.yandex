<div class="chat">
    {{#if chatName}}
        <div class="chat__header">
            {{{ChatHeader chatImg=chatImg chatName=chatName}}}
        </div>

        <div class="chat__content">
            {{#each messages}}
                {{{Message
                    text=this.text
                    img=this.img
                    date=this.date
                    time=this.time
                    senderId=this.senderId
                    isViewed=this.isViewed
                }}}
            {{/each}}
        </div>

        <div class="chat__send-wrap">
            {{{SendForm action=adminActions}}}
        </div>
    {{else}}
        <div class="chat__empty">
            Выберите чат чтобы отправить сообщение
        </div>
    {{/if}}
</div>
