<input
    class="input {{class}}{{#if isTopLabelPosition}} field--top-title{{/if}}"
    type="{{type}}"
    name="{{name}}"
    value="{{value}}"
    {{#if accept}}
        accept="{{accept}}"
    {{/if}}
    required
    {{#if isDisable}}
        disabled
    {{/if}}
/>
